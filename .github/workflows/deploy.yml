name: Deploy to EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:  # 수동 실행 가능

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to EC2
        env:
          PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
          HOST: ${{ secrets.EC2_HOST }}
          USER: ${{ secrets.EC2_USER }}
        run: |
          # SSH 키 설정
          echo "$PRIVATE_KEY" > private_key.pem
          chmod 600 private_key.pem
          
          # SSH 접속 및 배포
          ssh -o StrictHostKeyChecking=no -i private_key.pem ${USER}@${HOST} << 'ENDSSH'
            set -e
            
            # 프로젝트 디렉토리로 이동 (없으면 생성)
            mkdir -p ~/moviestar
            cd ~/moviestar
            
            # Git 저장소 초기화 (최초 실행 시)
            if [ ! -d .git ]; then
              git clone https://github.com/${{ github.repository }} .
            fi
            
            # 최신 코드 가져오기
            git fetch origin
            git reset --hard origin/main
            
            # 서버 초기 설정 (Docker 없을 경우 자동 설치)
            if ! command -v docker &> /dev/null; then
              echo "🔧 서버 초기 설정 실행..."
              chmod +x setup-server.sh
              ./setup-server.sh
            fi
            
            # Docker 컨테이너 중지 및 삭제
            sudo docker-compose down || true
            
            # 새 이미지 빌드 및 컨테이너 시작
            sudo docker-compose up -d --build
            
            # 컨테이너 시작 대기
            echo "⏳ 컨테이너 시작 대기 중..."
            sleep 10
            
            echo "📊 컨테이너 상태 확인:"
            sudo docker-compose ps
            
            # MongoDB 준비 확인 (더 robust하게)
            echo "🔍 MongoDB 준비 상태 확인 중..."
            MAX_RETRIES=30
            RETRY_COUNT=0
            
            while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
              echo "시도 $((RETRY_COUNT+1))/$MAX_RETRIES..."
              
              # mongosh로 연결 테스트 (에러 출력)
              if sudo docker-compose exec -T mongodb mongosh --quiet --eval "db.version()" 2>&1; then
                echo "✅ MongoDB 준비 완료!"
                break
              else
                echo "⚠️ MongoDB 아직 준비 안 됨, 2초 후 재시도..."
                sleep 2
                RETRY_COUNT=$((RETRY_COUNT+1))
              fi
            done
            
            if [ $RETRY_COUNT -eq $MAX_RETRIES ]; then
              echo "❌ MongoDB 준비 타임아웃. 로그 확인:"
              sudo docker-compose logs --tail=50 mongodb
              exit 1
            fi
            
            # DB 초기화 여부 확인
            echo "🔍 데이터베이스 초기화 확인 중..."
            DB_COUNT=$(sudo docker-compose exec -T mongodb mongosh --quiet --eval "db.getSiblingDB('dbjungle').movies.countDocuments()" 2>&1 | tail -1)
            echo "현재 영화 데이터 개수: $DB_COUNT"
            
            if [ "$DB_COUNT" = "0" ] || [ -z "$DB_COUNT" ]; then
              echo "📊 데이터베이스 초기화 시작..."
              sudo docker-compose exec -T app python init_db.py
              echo "✅ 데이터베이스 초기화 완료!"
            else
              echo "✅ 데이터베이스 이미 초기화되어 있음 (영화 수: $DB_COUNT개)"
            fi
            
            # 배포 완료 확인
            echo "🎉 배포 완료!"
            echo "📝 최근 앱 로그:"
            sudo docker-compose logs --tail=30 app
          ENDSSH
          
          # SSH 키 삭제
          rm -f private_key.pem

      - name: Verify Deployment
        env:
          HOST: ${{ secrets.EC2_HOST }}
        run: |
          echo "🔍 배포 검증 중..."
          sleep 3
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://${HOST}:5000 || echo "000")
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ 배포 성공! 애플리케이션이 정상 작동 중입니다."
            echo "🌐 URL: http://${HOST}:5000"
          else
            echo "⚠️ 배포 확인 필요 (HTTP Code: $HTTP_CODE)"
            exit 1
          fi
