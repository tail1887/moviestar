name: Deploy to EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:  # 수동 실행 가능

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to EC2
        env:
          PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
          HOST: ${{ secrets.EC2_HOST }}
          USER: ${{ secrets.EC2_USER }}
        run: |
          # SSH 키 설정
          echo "$PRIVATE_KEY" > private_key.pem
          chmod 600 private_key.pem
          
          # SSH 접속 및 배포
          ssh -o StrictHostKeyChecking=no -i private_key.pem ${USER}@${HOST} bash -s << 'ENDSSH'
            set -e
            
            # 프로젝트 디렉토리로 이동 (없으면 생성)
            mkdir -p ~/moviestar
            cd ~/moviestar
            
            # Git 저장소 초기화 (최초 실행 시)
            if [ ! -d .git ]; then
              git clone https://github.com/${{ github.repository }} .
            fi
            
            # 최신 코드 가져오기
            git fetch origin
            git reset --hard origin/main
            
            # 서버 초기 설정 (Docker 없을 경우 자동 설치)
            if ! command -v docker &> /dev/null; then
              echo "🔧 서버 초기 설정 실행..."
              chmod +x setup-server.sh
              ./setup-server.sh
            fi
            
            # Docker 컨테이너 중지 및 삭제
            sudo docker-compose down || true
            
            # 새 이미지 빌드 및 컨테이너 시작
            sudo docker-compose up -d --build
            
            # 컨테이너 시작 및 MongoDB 준비 대기
            echo "⏳ MongoDB 준비 대기 중..."
            sleep 15
            
            echo "📊 컨테이너 상태:"
            sudo docker-compose ps
            
            # DB 초기화 확인 (timeout 추가)
            echo "🔍 DB 초기화 확인..."
            set +e  # 일시적으로 에러 무시
            MOVIE_COUNT=$(timeout 10 sudo docker-compose exec -T app python check_db.py 2>/dev/null | tail -1 | tr -d '\r' || echo "0")
            set -e  # 다시 에러 체크 활성화
            echo "현재 영화 개수: [$MOVIE_COUNT]"
            
            if [ -z "$MOVIE_COUNT" ] || [ "$MOVIE_COUNT" = "0" ]; then
              echo "📊 DB 초기화 시작..."
              timeout 120 sudo docker-compose exec -T app python init_db.py || echo "⚠️ DB 초기화 타임아웃 또는 실패"
              echo "✅ DB 초기화 완료!"
            else
              echo "✅ DB 이미 초기화됨 (${MOVIE_COUNT}개)"
            fi
            
            echo "🎉 배포 완료!"
            sudo docker-compose ps
          ENDSSH
          
          # SSH 키 삭제
          rm -f private_key.pem

      - name: Verify Deployment
        env:
          HOST: ${{ secrets.EC2_HOST }}
        run: |
          echo "🔍 배포 검증 중..."
          sleep 3
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://${HOST}:5000 || echo "000")
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ 배포 성공! 애플리케이션이 정상 작동 중입니다."
            echo "🌐 URL: http://${HOST}:5000"
          else
            echo "⚠️ 배포 확인 필요 (HTTP Code: $HTTP_CODE)"
            exit 1
          fi
