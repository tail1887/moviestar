name: Deploy to EC2

on:
  push:
    branches:
      - main
  workflow_dispatch:  # 수동 실행 가능
    inputs:
      clear_db:
        description: '🧪 DB 초기화 테스트 (기존 데이터 삭제)'
        required: false
        type: choice
        options:
          - 'false'
          - 'true'
        default: 'false'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to EC2
        env:
          PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
          HOST: ${{ secrets.EC2_HOST }}
          USER: ${{ secrets.EC2_USER }}
        run: |
          # SSH 키 설정
          echo "$PRIVATE_KEY" > private_key.pem
          chmod 600 private_key.pem
          
          # SSH 접속 및 배포
          ssh -o StrictHostKeyChecking=no -i private_key.pem ${USER}@${HOST} bash -s << 'ENDSSH'
            set -e
            
            # 프로젝트 디렉토리로 이동 (없으면 생성)
            mkdir -p ~/moviestar
            cd ~/moviestar
            
            # Git 저장소 초기화 (최초 실행 시)
            if [ ! -d .git ]; then
              git clone https://github.com/${{ github.repository }} .
            fi
            
            # 최신 코드 가져오기
            git fetch origin
            git reset --hard origin/main
            
            # 서버 초기 설정 (Docker 없을 경우 자동 설치)
            if ! command -v docker &> /dev/null; then
              echo "🔧 서버 초기 설정 실행..."
              chmod +x setup-server.sh
              ./setup-server.sh
            fi
            
            # Docker 컨테이너 중지 및 삭제
            sudo docker-compose down || true
            
            # 매번 DB 초기화 실행 (CLEAR_DB 환경변수 설정하여 app.py에서 자동 초기화)
            echo "🔄 DB 초기화 모드로 빌드 및 시작..."
            export CLEAR_DB=true
            sudo -E docker-compose up -d --build
            echo "✅ 앱이 CLEAR_DB 모드로 시작되어 자동 초기화 중..."
            
            # 컨테이너 시작 대기
            echo "⏳ 컨테이너 시작 대기 중..."
            sleep 15
            
            echo "🎉 배포 완료!"
            echo "📊 컨테이너 상태:"
            sudo docker-compose ps
            
            echo "📝 앱 시작 로그 (DB 자동 초기화 확인):"
            sudo docker-compose logs --tail=40 app
          ENDSSH
          
          # SSH 키 삭제
          rm -f private_key.pem

      - name: Verify Deployment
        env:
          HOST: ${{ secrets.EC2_HOST }}
        run: |
          echo "🔍 배포 검증 중..."
          sleep 3
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://${HOST}:80 || echo "000")
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ 배포 성공! 애플리케이션이 정상 작동 중입니다."
            echo "🌐 URL: http://${HOST}"
          else
            echo "⚠️ 배포 확인 필요 (HTTP Code: $HTTP_CODE)"
            exit 1
          fi
